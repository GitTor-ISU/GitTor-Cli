name: Setup

on:
  workflow_call:

jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y clang-tidy valgrind gcovr libtorrent-rasterbar-dev
          pip install cpplint
